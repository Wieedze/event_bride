<!DOCTYPE html>
<html lang="fr">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Attendees de l'Événement</title>

  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha1/dist/css/bootstrap.min.css" rel="stylesheet">
</head>
<body>
  <div class="container mt-5">
    <h1 class="text-center">Participant de l'événement : </h1>


    <table class="table table-striped">
      <thead>
        <tr>
          <th scope="col">#</th>
          <th scope="col">Nom</th>
          <th scope="col">Email</th>
          <th scope="col">Date d'inscription</th>
        </tr>
      </thead>
      <tbody>

        <% @attendees.each_with_index do |attendance, index| %>
          <tr>
            <th scope="row"><%= index + 1 %></th>
            <td><%= attendance.user.first_name %></td>
            <td><%= attendance.user.email_address %></td>
            <td><%= attendance.created_at.strftime("%d/%m/%Y") %></td>
          </tr>
        <% end %>
      </tbody>
    </table>


    <div class="text-center">
      <a href="<%= event_path(@event) %>" class="btn btn-primary">Retour à l'événement</a>
    </div>
    <div class="text-center">
      <%= link_to "Editer mon evenement", edit_event_path(@event), class: "btn btn-secondary" %>
    </div>
  </div>



  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha1/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
